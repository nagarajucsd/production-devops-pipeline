Projeect Documentation:

Module1: Foundation

step1: Started with creating github repo "production-devops-pipeline" without intializing README, .gitignore and License.
step2: Created a project folder in wsl ubuntu (local laptop) "production-devops-pipeline" and into the folder.
step3: Intialized the git for the project folder
step4: now created the project folder structure with required folder like (app, docker, kubernetes, terraform, jenkins, monitoring, ngnix, scripts, diagrams, screenshots, docs)
step5: created README and wrote basic data into it (local)
step6: created .gitignore and wrote basic details in it (local)
step7: now add the files and commited first commit, rename branch to main, connect to github, push the code to the github repo.

errors faced: 
1. while pushing the code encountered an eror for different github details and different repo. also problem with the public and private key.
sol: 
  1.verified the keys and removed those (cd ~/.ssh, ls -l, rm id_ed25519 id_ed25519.pub)
  2.created the ssh key again (ssh-keygen -t ed25519 -C "nagaraju_csd@srkrec.edu.in" verify: ls -al ~/.ssh )
  3. get the public key (cat ~/.ssh/id_ed25519.pub) and  put it in GitHub → Settings → SSH and GPG keys → New SSH key
  4. test the connection (ssh -T git@github.com)
  5. changed the repo remote from https to ssh 
links:(https://chatgpt.com/c/6a42028f-f410-83ee-b54d-aa896689cd1a)



Module2: Build a Java Spring boot Application

step1: create a spring boot project from https://start.spring.io/ with dependencies (Spring Web, Boot Actuator, boot devtools) and download the zip and extract it in the project folder /app
step2: open the application in vscode (code .) and run the application 
step3: create rest controller inside src/main/java/com/devops/springbootapp folder /controller file HomeController.java
step4: test it (http://localhost:8081) and and check whether it is health ans its version also
step5: maven build package (creating the jar file)
step6: adding the changes in project folder and pushing it to the github

errors faced:
1. encountered with error port 8080 in use 
sol : tried in deleting the process id related to port 8080 again and again without checking what's the error is exactly then realised that  it(8080) already assigned to jenkins so changed the spring boot port to 8081 (In appliacation.properties: server.port=8081)
    and restarted the application
2. 404 Not Found No static resource health for request '/health'  
sol: configured acutation in application.properties   management.endpoints.web.exposure.include=health,info  --> This tells Spring Boot which Actuator endpoints should be accessible over HTTP.
                                                      management.endpoint.health.show-details=always  -->  This controls how much information the /actuator/health endpoint returns.
enabled mapping logs ::  logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=TRACE --> tells Spring Boot to log every URL mapping that it registers when the application starts.
 also in pom.xml added the dependency 
  (<dependency>
    <groupId>org.springframework.boot</groupId>         --> It turns  Spring Boot project into a web application 
    <artifactId>spring-boot-starter-web</artifactId>         by adding everything needed to build REST APIs and web applications.
  </dependency>)                                             spring-boot-starter-web is a bundle of web-related libraries.




Module3: Docker 

step1: verify docker with docker --version and docker info 
step2: go to app folder springboot folder and build the jar (mvn clean package)  and verify in target folder 
step3: create and write the Dockerfile in the project root folder itself 
step4: build docker image and run docker container 
step5: verify whether container is running or not and also the application is working or not , then enter the container stop and start the contaier finally remove teh container and image.
step6: then push changes to the repo

errros faced: 
1. docker --version docker commond not found ( as i didn't started the docker desktop at that time)
sol : after starting the docker desktop it showed the docker version
2. while building the image encountered error (ERROR: docker: 'docker buildx build' requires 1 argument)
because mistake in command (docker build -t springboot-app:v1 ) missed "." at end
sol: docker build -t springboot-app:v1 .  (as the Dockerfile is in the project root folder )


Module4: Jenkins CI/CD pipeline

step1: Install jenkins in local or install using docker and login to jenkins
step2: install plugins and create a jenkins pipeline
step3: create a Jenkinsfile in the project root folder and add code to it
step4: configure pipeline git repo, branch main, script path  and build the pipeline
step5: verify the springboot application and push the changes to the github repo

errors faced:
1. Failed to restart docker.service: Unit docker.service not found.(Docker desktop is not opened)
sol: started the docker desktop
2. git rev-parse refs/remotes/origin/master^{commit} ERROR: Couldn't find any revision to build.
sol: jenkins trying to fetch master but my branch is main so modified the jenkins pipeline code 
    stage('Checkout') {
    steps {
        git branch: 'main',
            url: 'https://github.com/nagarajucsd/production-devops-pipeline.git'
    }
    }

3. ./mvnw: Permission denied 
sol : execute permission is not given for mvnw so modified it in both local system and jenkins pipeline
  stage('Build') {
    steps {
        dir('app/springboot-app') {
            sh '''
                chmod +x mvnw
                ./mvnw clean package
            '''
        }
    }
  }



Module5: AWS Deployment

step1: launch EC2 instance (ubuntu 24.04, t3micro -free tier, 20GB, key-pair) Security group - port allowed (ssh- 22, application-8081)
step2: connect to ec2 via ssh through wsl (ssh -i ~/.ssh/key-pair.pem ubuntu@ip-address)
step3: update and upgrade the package 
step4: install docker, enable docker , docker status and check docker version . docker permissions (sudo usermod -aG docker ubuntu) docker ps works without sudo.
step5: push image to docker hub from local 
step6: pull the image on ec2 and run the container
step7: test the browser http://ec2-ipaddress:8081
step8: add, commit and push  the changes to the github

errors faced:
1.  WARNING: UNPROTECTED PRIVATE KEY FILE! while connecting ec2 using (ssh -i key-pair.pem ubuntu@ec2-ipaddress)
sol: as i tried to connect it from wsl /mnt/c      as the key is using windows permissions so used the key from linux home directory as (ssh -i ~/.ssh/key-pair.pem ubuntu@ip-address)
2. ! [rejected]        main -> main (fetch first)  -->It means the remote branch contains commits that the local branch doesn't have. Git rejects the push to prevent accidentally 
                                                   overwriting remote history. The usual solution is to fetch and integrate the remote changes (often with git pull --rebase) before pushing.
sol: git log --oneline --graph --decorate --all -10  and   git pull --rebase origin main --> to know the conflicts.
updated the jenkins file and add it to the github and git rebase --continue save the file and exit editor  and push the code again


