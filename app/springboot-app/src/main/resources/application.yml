server:
  port: 8082

spring:
  application:
    name: production-devops-pipeline

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

  endpoint:
    health:
      show-details: always

info:
  app:
    name: Production DevOps Pipeline
    version: 1.0